name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install -e ".[dev]"
      - run: python -m ruff check .
      - run: python -m ruff format --check .
      - run: python -m mypy --strict src
      - run: python -m pytest --cov=agent_policy_pack --cov-branch
      - run: python -m bandit -r src

  package:
    name: Package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - run: python -m pip install -e ".[dev]"
      - run: python -m pip_audit
      - run: python -m build
      - run: python -m twine check dist/*
      - name: Clean wheel install smoke test
        run: |
          python -m venv /tmp/agentpolicypack-wheel-smoke
          /tmp/agentpolicypack-wheel-smoke/bin/python -m pip install --upgrade pip
          /tmp/agentpolicypack-wheel-smoke/bin/python -m pip install dist/*.whl
          /tmp/agentpolicypack-wheel-smoke/bin/python -c "import agent_policy_pack; print(agent_policy_pack.__version__)"
          /tmp/agentpolicypack-wheel-smoke/bin/agentpolicy --help
